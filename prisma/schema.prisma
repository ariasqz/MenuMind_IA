datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model MenuGenerado {
  id           Int      @id @default(autoincrement())
  ingredientes String
  sugerencias  String
  fecha        DateTime @default(now())
}